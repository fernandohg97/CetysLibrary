import { SearchCareerPipe } from './search-career.pipe';

describe('SearchCareerPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchCareerPipe();
    expect(pipe).toBeTruthy();
  });
});
