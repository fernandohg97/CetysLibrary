import { SearchReservationDatePipe } from './search-reservation-date.pipe';

describe('SearchReservationDatePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchReservationDatePipe();
    expect(pipe).toBeTruthy();
  });
});
