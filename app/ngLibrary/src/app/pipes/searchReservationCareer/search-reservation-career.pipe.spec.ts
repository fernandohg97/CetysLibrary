import { SearchReservationCareerPipe } from './search-reservation-career.pipe';

describe('SearchReservationCareerPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchReservationCareerPipe();
    expect(pipe).toBeTruthy();
  });
});
