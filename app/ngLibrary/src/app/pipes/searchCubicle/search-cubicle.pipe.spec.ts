import { SearchCubiclePipe } from './search-cubicle.pipe';

describe('SearchCubiclePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchCubiclePipe();
    expect(pipe).toBeTruthy();
  });
});
