import { SearchReservationCubiclePipe } from './search-reservation-cubicle.pipe';

describe('SearchReservationCubiclePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchReservationCubiclePipe();
    expect(pipe).toBeTruthy();
  });
});
