// To know which admin element you want to delete all documents
// This is called when you want to remove all files
// For example if you press the button to remove all the students.
export enum AdminSection {
  students = 0,
  cubicles = 1,
  employees = 2,
  careers = 3,
  departments = 4,
  reservations = 5
}
